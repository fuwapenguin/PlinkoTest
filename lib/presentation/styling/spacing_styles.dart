class SpacingStyles {
  /// 4 units.
  ///
  /// Used for making space for elements that belong together.
  static const double extraSmallSpacing = 4;
  /// 8 units.
  ///
  /// Used for creating space for grouped elements.
  static const double smallSpacing = 8;
  /// 16 units.
  ///
  /// Used for creating a new section inside a card or container.
  static const double mediumSpacing = 16;
  /// 16 units.
  ///
  /// Used for padding inside of cards or containers.
  static const double containerPadding = 16;
  /// 24 units.
  ///
  /// Used for creating new sections between cards or containers.
  static const double largeSpacing = 24;
  /// 32 units.
  ///
  /// Used for making extra breathing space.
  static const double extraLargeSpacing = 32;
}